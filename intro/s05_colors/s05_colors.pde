size(600,500);
//fill(255,0,0); // max red, no green, no blue
fill(0,255,0); // no red, max green, no blue
//fill(255,0,0); // max red, no green, no blue
fill(50); // same as fill(50,50,50) (dark gray)
rect(0,0, 200,100);